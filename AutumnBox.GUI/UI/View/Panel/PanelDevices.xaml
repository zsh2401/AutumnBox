<UserControl x:Class="AutumnBox.GUI.UI.View.Panel.PanelDevices"
            
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AutumnBox.GUI.UI.View.Panel"
             mc:Ignorable="d" Width="177.009" Height="303.769">
    <GroupBox Header="{DynamicResource Devices}">
        <Grid>
            <ListBox x:Name="MainList" Margin="10,0,10,121"  DisplayMemberPath="Serial" ItemsSource="{Binding Model.Devices}" SourceUpdated="MainList_SourceUpdated" SelectionChanged="ListBox_SelectionChanged"/>
            <Button Content="{DynamicResource btnAddNetdebuggingDevice}" IsEnabled="{Binding Model.BtnConnectEnable}" Command="{Binding ConnectDevice}" Margin="10,0,10,84" Height="32" VerticalAlignment="Bottom" FontSize="12"/>
            <Button Content="{DynamicResource btnEnableNetdebugging}" Command="{Binding OpenDeviceNetDebugging}" Margin="10,0,10,47" Height="32" VerticalAlignment="Bottom" FontSize="12"/>
            <Button Content="{DynamicResource btnDisableNetdebugging}" Command="{Binding DisconnectDevice}" Margin="10,0,10,10" VerticalAlignment="Bottom" FontSize="12"/>
        </Grid>
    </GroupBox>
</UserControl>
