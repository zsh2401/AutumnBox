<UserControl x:Class="AutumnBox.GUI.UI.CstPanels.WelcomeGrid"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AutumnBox.GUI.UI.Grids"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="988" OpacityMask="Black">
    <Grid>
        <Label Content="{DynamicResource ThankMessage1}" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top" FontSize="36" Foreground="{DynamicResource ForgBrush}"/>
        <Label Content="{DynamicResource ThankMessage2}" HorizontalAlignment="Left" Margin="10,71,0,0" VerticalAlignment="Top" FontSize="20" Foreground="{DynamicResource ForgBrush}"/>
        <Label Content="{DynamicResource ThankMessage3}" HorizontalAlignment="Left" Margin="10,111,0,0" VerticalAlignment="Top" FontSize="20" Foreground="{DynamicResource ForgBrush}"/>
    </Grid>
</UserControl>
