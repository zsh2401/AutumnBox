<UserControl x:Class="AutumnBox.GUI.View.DialogContent.LanguageChoice"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AutumnBox.GUI.View.DialogContent"
             xmlns:viewmodel="clr-namespace:AutumnBox.GUI.ViewModel"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             FontFamily="{DynamicResource AutumnBox.Font}"
             >
    <UserControl.DataContext>
        <viewmodel:VMLanguageChoice/>
    </UserControl.DataContext>
    <StackPanel Margin="10">
        <TextBlock FontSize="18" TextAlignment="Center">选择语言</TextBlock>
        <TextBlock FontSize="18" TextAlignment="Center">Choice Your Language</TextBlock>
        <ComboBox DisplayMemberPath="LangName" Margin="0,25,0,25" ItemsSource="{Binding Languages}" SelectedItem="{Binding SelectedLanguage}"/>
        <Button Style="{DynamicResource MaterialDesignFlatButton}" Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}" Content="{DynamicResource btnClose}"/>
    </StackPanel>
</UserControl>
