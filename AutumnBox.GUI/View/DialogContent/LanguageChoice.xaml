<UserControl x:Class="AutumnBox.GUI.View.DialogContent.LanguageChoice"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AutumnBox.GUI.View.DialogContent"
             xmlns:viewmodel="clr-namespace:AutumnBox.GUI.ViewModel"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             FontFamily="{DynamicResource AutumnBox.Font}">
    <UserControl.DataContext>
        <viewmodel:VMLanguageChoice/>
    </UserControl.DataContext>
    <Grid Margin="10">
        <StackPanel>
            <TextBlock TextAlignment="Center" Text="选择你的语言"/>
            <TextBlock TextAlignment="Center" Text="Select Your Language"/>
            <ListView DisplayMemberPath="LangName" Height="100" Width="200"  ItemsSource="{Binding Languages}" SelectedItem="{Binding SelectedLanguage}"/>
            <Button HorizontalAlignment="Right" Style="{DynamicResource MaterialDesignToolButton}" Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}" Content="{DynamicResource btnOK}"/>
        </StackPanel>
    </Grid>
</UserControl>
