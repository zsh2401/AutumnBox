<UserControl x:Class="AutumnBox.GUI.Views.SubWindowViews.LeafUIYNView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:system="clr-namespace:System;assembly=mscorlib"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             FontFamily="{DynamicResource AutumnBox.Font}"
             Background="{DynamicResource RegionBrush}"
             mc:Ignorable="d" 
             MaxWidth="300"
             MaxHeight="250"
               MinWidth="170" MinHeight="130"
             >
    <DockPanel >
        <hc:ButtonGroup DockPanel.Dock="Bottom" Margin="10">
            <Button Click="BtnNo_Click" IsCancel="True" x:Name="BtnNo" MinWidth="70"  Content="{DynamicResource ChoiceWindowBtnNo}"/>
            <Button Click="BtnYes_Click" IsDefault="True" x:Name="BtnYes" Content="{DynamicResource ChoiceWindowBtnYes}"/>
        </hc:ButtonGroup>
        <TextBlock Padding="10" DockPanel.Dock="Top" TextWrapping="Wrap" x:Name="TBContent" />

        <!--<hc:Divider DockPanel.Dock="Bottom" />-->
    </DockPanel>
</UserControl>
