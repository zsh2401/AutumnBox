<UserControl x:Class="AutumnBox.GUI.Views.SubWindowViews.LeafUIMessageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             Background="White"
             MinWidth="150"
             MinHeight="100"
             >
    <Border CornerRadius="10">
        <DockPanel>
            <Button Click="Button_Click" IsCancel="True" IsDefault="True"  Margin="10" DockPanel.Dock="Bottom" Content="{DynamicResource OpenFxBtnYes}"/>
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <TextBlock Padding="10" TextWrapping="Wrap" x:Name="TBContent"/>
            </ScrollViewer>
        </DockPanel>
    </Border>
</UserControl>
