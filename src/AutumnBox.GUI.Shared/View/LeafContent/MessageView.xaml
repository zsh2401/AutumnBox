<UserControl x:Class="AutumnBox.GUI.View.LeafContent.MessageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodel="clr-namespace:AutumnBox.GUI.ViewModel"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             Background="White"
             >
    <Border CornerRadius="10">
        <DockPanel>
            <Button Click="Button_Click" IsCancel="True" IsDefault="True"  Margin="10" DockPanel.Dock="Bottom">OK</Button>
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <TextBlock TextWrapping="Wrap" x:Name="TBContent"/>
            </ScrollViewer>
        </DockPanel>
    </Border>
</UserControl>
